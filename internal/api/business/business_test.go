package business

import (
	"fmt"
	"github.com/Pallinder/go-randomdata"
	"testing"
)

func TestGetBusinessList(t *testing.T) {
	//fmt.Println(unsafe.Sizeof(api.BusinessApiKeyInfo{}))
	fmt.Print(randomdata.RandStringRunes(32))
}
